# CS업무일지 변경 이력

## v3.6.3 (2026-08-04) — 관리자 조회 화면 케이스 항목 누락 수정

### 배경
아래 "현장업무(현장지원) 항목 재구성" 작업의 "미반영 범위"에 있던 문제가 실제로 보고됨:
직원이 보고서 작성 시 입력한 "이전 방문자 AS처리내용 / 현장 고객반응 / 방문자 초기진단"(2~4번 항목)이
관리자 모드에서 보고서를 확인할 때 표시되지 않음.

### 원인
텔레그램 공유(`buildReportText`, `buildDailyText`)와 수정 화면(`buildAECases` 등)에는
2~4번 항목이 반영되어 있었지만, 관리자용 조회/집계 화면은 반영되지 않은 채 남아있었음.

### 변경 내용
다음 5개 함수의 케이스 표시 로직에 이전AS/고객반응/초기진단 행을 추가:
- `renderRpt()` — 일일보고
- `renderWk()` — 주간보고
- `renderMonthly()` — 월간 담당자별 상세
- `doSearch()` — 검색 결과
- `renderDetEntries()` — 직원별 상세

각 화면의 케이스 필터 조건(`c.model||c.symptom||c.note`)에도 `||c.prevAs||c.custResp||c.diag`를 추가하여,
1·5·6번 항목 없이 2~4번 항목만 입력된 케이스도 목록에서 누락되지 않도록 수정.

부수적으로 월간(`renderMonthly`)/검색(`doSearch`) 화면의 테이블 헤더 컬럼 수가 실제 데이터 컬럼 수(기종구분 누락)와
맞지 않아 데이터가 한 칸씩 밀려 표시되던 문제도 함께 수정.

### 배포
- 저장소: `kiw2673-png/work-journal` (GitHub REST API PUT으로 업로드)
- 배포 URL: `https://kiw2673-png.github.io/work-journal/index.html`

---

## 2026-08-04 — 현장업무(현장지원) 항목 재구성

### 배경
현장지원 케이스 입력 항목(증상 / 처리결과 / 조치내용)을 6단계 구조로 세분화하여
접수부터 처리결과까지 흐름이 명확히 드러나도록 변경.

### 변경 내용

| 순서 | 항목명 | 이전 | 비고 |
|---|---|---|---|
| 1 | 최초 접수내용 | "증상" | 기존 선택박스+태그+직접입력 방식 유지, 라벨만 변경 |
| 2 | 이전 방문자 AS처리내용 | (신규) | 텍스트칸 신규 추가, 예시: "스파크플러그, 화염감지기, 자동점화기, 가스밸브 교체." |
| 3 | 현장 고객반응 | (신규) | 텍스트칸 신규 추가, 예시: "고객이 간헐적으로 소음난다고 함." |
| 4 | 방문자 초기진단 | (신규) | 텍스트칸 신규 추가, 예시: "보일러 가동을 시켜 점화시도 중 부릉부릉소음 발생, 가동시 간헐적 소음이 발생됨." |
| 5 | 방문자 처리내용 | "조치내용" | 기존 텍스트칸 라벨 변경, 예시: "연소실 스파크플러그 세트를 전동드라이버로 분리하고..." |
| 6 | 처리결과 | "처리결과" | 기존 선택박스를 맨 아래로 이동, 라벨만 변경 |

- 모든 예시 문구는 placeholder로 처리되어 텍스트 입력 시 자동으로 사라짐.
- 케이스(현장지원)를 하나라도 작성 시 1~6번 항목 전체가 **필수 입력**으로 지정됨.

### 필수 입력 검증
- `doSave()` 제출 시 `getMissingCaseFields()`로 누락 항목 검사.
- 최초에는 "저장 후 알림"으로 구현했으나, 이후 요청에 따라 **누락 항목이 있으면 제출 자체를 막고 alert만 표시**하도록 변경 (2026-08-04 2차 수정).

### 함께 수정된 연동 부분
- `buildReportText()` — 텔레그램/카톡 공유 텍스트에 1~6번 항목 전체 표시
- `buildDailyText()` — 관리자 일일 자동 보고(텔레그램)에도 신규 항목 반영
- `buildAECases()` / `getAECases()` / `saveAdminEdit()` / `saveUserEdit()` — 보고서 수정(관리자/본인) 화면에도 동일 항목 반영, 신규 필드 유실 방지

### 미반영 범위 (참고)
- 통계, 엑셀/PPT 내보내기, 검색 등 집계성 화면은 이번 작업에서 다루지 않음 — 기존 필드명(symptom/action/note) 그대로 사용 중.

### 배포
- 저장소: `kiw2673-png/work-journal` (GitHub REST API PUT으로 업로드)
- 배포 URL: `https://kiw2673-png.github.io/work-journal/index.html`
